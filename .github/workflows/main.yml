name: SSH Command Execution

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: SSH command execution
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: 103.126.238.246
          username: stai
          password: annadwah2024
          port: 2244
          script: |
            # Deploy aplikasi ke server
            cd /home
            mkdir ok2
